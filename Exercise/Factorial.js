function factorial(n){
    // this funciton will help you to find the factorial of the given "n" number "Factorial/n!".
    let total = 1;
    for (let i=1; i<=n; i++);
    total = total*i //another way to put total *= i;
    return total; 
};

factorial();